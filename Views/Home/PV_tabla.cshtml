

<div class="row">
    <div class="col-md-12">
        <table class="table table-striped">
            <tr>
                <th>
                    Imagen
                </th>

                <th>
                    Fecha Cargada
                </th>

                <th>

                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @*<img  style="width: 50px; height: 50px;" src="~/ImagenesPacientes/@item.idPaciente-@item.NombrePaciente.Trim()-@item.ApellidoPaterno.Trim()-@item.ApellidoMaterno.Trim()/@item.fileName.Trim()"/>*@
                        <img style="width: 50px; height: 50px;" src="~/ImagenesPacientes/@item.idPaciente/@item.fileName.Trim()" />
                    </td>
                    <td>
                        @item.fechaCargada
                    </td>

                    <td>
                        <a href="javascript:eliminarImagen('@item.fileName',@item.idImagenPaciente)" class="btn btn-danger">Eliminar</a>
                    </td>

                </tr>

            }
        </table>
    </div>
</div>

